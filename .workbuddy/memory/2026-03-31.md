# 2026-03-31 工作日志

## 项目 API 版本升级（API 20 → API 21）
- 修改 `build-profile.json5`：`targetSdkVersion` 和 `compatibleSdkVersion` 从 `"6.0.0(20)"` 改为 `"6.0.1(21)"`
- API 21 对应 HarmonyOS 6.0.1，需在 DevEco Studio SDK Manager 中手动下载 6.0.1 SDK

## ActivityPage.ets 美化（AI新浪潮风格）
- 完成了 `entry/src/main/ets/pages/ActivityPage.ets` 的视觉重设计
- 设计风格：深色科技感（Dark AI Wave），主色调 `#0D111C` 深底色
- 主要改动：
  - 顶部 Hero 区：渐变背景 + 装饰性高斯模糊光晕，党红 `#C0392B` 与科技蓝 `#60CFFF` 双光晕
  - 发布按钮：红色渐变 + 发光阴影
  - Tab 栏：深底暗线分割，选中态霓虹红渐变指示条
  - 活动卡片：封面图全宽裁切，底部渐变遮罩，内容区磨砂玻璃深色背景 `#1A1F2E`
  - 状态角标：独立 `StatusBadge` 组件，蓝/绿/灰三色 + 呼吸点
  - 报名进度条：红色渐变进度条，实时比例填充
  - 操作按钮：根据状态切换磨砂玻璃轮廓按钮（蓝/绿/红），高亮按钮带发光阴影
  - 空状态：带光圈的 emoji 占位图
  - 加载状态：磨砂玻璃卡片包裹 LoadingProgress
- 业务逻辑完整保留，lint 检查零错误
